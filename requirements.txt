# DBC parsing
cantools>=39.0

# Data handling
pandas>=2.0
pyarrow>=14.0

# Training (QLoRA via Unsloth)
unsloth[colab-new]>=2024.0
trl>=0.7
transformers>=4.38
datasets>=2.16
accelerate>=0.26
bitsandbytes>=0.42
peft>=0.8

# Inference
# vllm>=0.3  # install separately, heavy dependency

# Eval & utils
scikit-learn>=1.3
tqdm>=4.66
pyyaml>=6.0
